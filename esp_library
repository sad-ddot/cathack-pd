local ESPLib = {}
ESPLib.__index = ESPLib
ESPLib._version = "0.3.0"

local RunService = game:GetService("RunService")
local Players    = game:GetService("Players")
local Workspace  = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local function wtvp(pos)
    local sp, on = Camera:WorldToViewportPoint(pos)
    return sp, on
end

local function get_hum_hrp(model)
    if not model or not model.Parent then return nil, nil end
    local hum = model:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then return nil, nil end
    local hrp = model:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil, nil end
    return hum, hrp
end

local function new_drawing(kind, props)
    local d = Drawing.new(kind)
    if props then
        for k, v in pairs(props) do d[k] = v end
    end
    return d
end

local function destroy_all(objs)
    for k, d in pairs(objs) do
        if d then d:Remove() end
        objs[k] = nil
    end
end

local skeleton_bones = {
    { "Head", "UpperTorso" },
    { "UpperTorso", "LowerTorso" },
    { "UpperTorso", "LeftUpperArm" }, { "LeftUpperArm", "LeftLowerArm" }, { "LeftLowerArm", "LeftHand" },
    { "UpperTorso", "RightUpperArm" }, { "RightUpperArm", "RightLowerArm" }, { "RightLowerArm", "RightHand" },
    { "LowerTorso", "LeftUpperLeg" }, { "LeftUpperLeg", "LeftLowerLeg" }, { "LeftLowerLeg", "LeftFoot" },
    { "LowerTorso", "RightUpperLeg" }, { "RightUpperLeg", "RightLowerLeg" }, { "RightLowerLeg", "RightFoot" },
}

local BODY_PARTS = {
    "Head", "UpperTorso", "LowerTorso",
    "LeftUpperArm", "LeftLowerArm", "LeftHand",
    "RightUpperArm", "RightLowerArm", "RightHand",
    "LeftUpperLeg", "LeftLowerLeg", "LeftFoot",
    "RightUpperLeg", "RightLowerLeg", "RightFoot",
}

local DEFAULT_STYLE = {
    enabled = true,
    max_distance = 3000,

    teamcheck = false,
    friendcheck = false,
    include_ai = true,

    box = { enabled = true, thickness = 1, color = Color3.fromRGB(255,255,255), transparency = 1 },
    box_outline = { enabled = true, thickness = 3, color = Color3.fromRGB(0,0,0), transparency = 1 },
    box_fill = { enabled = false, color = Color3.fromRGB(0,0,0), transparency = 0.35 },

    name = { enabled = true, size = 13, font = 2, color = Color3.fromRGB(255,255,255), outline = true, outline_color = Color3.fromRGB(0,0,0) },
    displayname = { enabled = false, size = 12, font = 2, color = Color3.fromRGB(200,200,200), outline = true, outline_color = Color3.fromRGB(0,0,0) },
    distance = { enabled = true, size = 12, font = 2, color = Color3.fromRGB(200,200,200), outline = true, outline_color = Color3.fromRGB(0,0,0), suffix = "m" },
    weapon = { enabled = false, size = 12, font = 2, color = Color3.fromRGB(200,200,200), outline = true, outline_color = Color3.fromRGB(0,0,0) },

    health = {
        enabled = true,
        thickness = 3,
        outline_thickness = 1,
        outline_color = Color3.fromRGB(0,0,0),
        color_full = Color3.fromRGB(90, 220, 90),
        color_low = Color3.fromRGB(220, 90, 90),
        show_text = false,
        text_size = 11,
        text_font = 2,
    },

    skeleton = { enabled = false, color = Color3.fromRGB(255,255,255), thickness = 1 },

    chams = {
        enabled = false,
        style = "Glow",
        hidden_color = Color3.fromRGB(152, 188, 255),
        hidden_transparency = 0.5,
        visible_color = Color3.fromRGB(152, 188, 255),
        visible_transparency = 0.5,
    },

    visible_color = nil,
    hitscan_color = nil,

    off_screen_arrow = { enabled = false, size = 20, color = Color3.fromRGB(255,80,80), radius = 200 },

    loot = {
        enabled = false,
        max_distance = 250,
        color = Color3.fromRGB(255, 220, 80),
        text_size = 12,
        text_font = 2,
        outline = true,
        outline_color = Color3.fromRGB(0, 0, 0),
        show_distance = true,
        min_worth = 0,
    },

    trail = {
        enabled = false,
        color = Color3.fromRGB(255, 255, 255),
        segment_life = 3.0,
        max_segments = 40,
        thickness = 2,
        transparency = 0.4,
    },
}

local function deep_merge(dst, src)
    for k, v in pairs(src) do
        if type(v) == "table" and type(dst[k]) == "table" then
            deep_merge(dst[k], v)
        else
            dst[k] = v
        end
    end
end

function ESPLib.new(config)
    local self = setmetatable({}, ESPLib)
    self.style = {}
    deep_merge(self.style, DEFAULT_STYLE)
    if config then deep_merge(self.style, config) end

    self._entries = {}
    self._loot_entries = {}
    self._trail_data = {}
    self._connections = {}
    self._is_target_fn = nil
    self._visible_check_fn = nil
    self._hitscan_check_fn = nil
    self._weapon_getter = nil
    self._loot_root_getter = nil
    self._loot_worth_getter = nil
    self._last_loot_scan = 0

    return self
end

function ESPLib:SetTargetFilter(fn) self._is_target_fn = fn end
function ESPLib:SetVisibleCheck(fn) self._visible_check_fn = fn end
function ESPLib:SetHitscanCheck(fn) self._hitscan_check_fn = fn end
function ESPLib:SetWeaponGetter(fn) self._weapon_getter = fn end
function ESPLib:SetLootRoot(fn) self._loot_root_getter = fn end
function ESPLib:SetLootWorth(fn) self._loot_worth_getter = fn end

function ESPLib:_should_track(subject, is_npc)
    if is_npc then return self.style.include_ai end
    if subject == LocalPlayer then return false end
    if self._is_target_fn then
        local ok, r = pcall(self._is_target_fn, subject, is_npc)
        if ok then return r == true end
    end
    return true
end

function ESPLib:_get_model(subject, is_npc)
    if is_npc then return subject end
    return subject.Character
end

function ESPLib:_create_entry(subject, is_npc)
    local entry = {
        subject = subject,
        is_npc = is_npc,
        objs = {
            box         = new_drawing("Square", { Filled = false, Thickness = 1, Visible = false, ZIndex = 3 }),
            box_outline = new_drawing("Square", { Filled = false, Thickness = 3, Visible = false, ZIndex = 2 }),
            box_fill    = new_drawing("Square", { Filled = true,  Visible = false, ZIndex = 1 }),
            name        = new_drawing("Text", { Center = true, Visible = false, ZIndex = 4 }),
            displayname = new_drawing("Text", { Center = true, Visible = false, ZIndex = 4 }),
            distance    = new_drawing("Text", { Center = true, Visible = false, ZIndex = 4 }),
            weapon      = new_drawing("Text", { Center = true, Visible = false, ZIndex = 4 }),
            hp_bar      = new_drawing("Line", { Visible = false, ZIndex = 3 }),
            hp_outline  = new_drawing("Line", { Visible = false, ZIndex = 2 }),
            hp_text     = new_drawing("Text", { Center = false, Visible = false, ZIndex = 4 }),
            arrow       = new_drawing("Triangle", { Filled = true, Visible = false, ZIndex = 3 }),
        },
        skel_lines = {},
        adornments = {},
    }
    return entry
end

function ESPLib:_destroy_entry(entry)
    destroy_all(entry.objs)
    for _, ln in pairs(entry.skel_lines) do
        if ln then ln:Remove() end
    end
    entry.skel_lines = {}
    for _, pair in pairs(entry.adornments) do
        for _, a in ipairs(pair) do
            if a and a.Parent then a:Destroy() end
        end
    end
    entry.adornments = {}
end

function ESPLib:_ensure_skel(entry)
    if not self.style.skeleton.enabled then
        for _, ln in pairs(entry.skel_lines) do ln.Visible = false end
        return
    end
    if next(entry.skel_lines) == nil then
        for i = 1, #skeleton_bones do
            entry.skel_lines[i] = new_drawing("Line", { Visible = false, ZIndex = 3 })
        end
    end
end

local function color_multiply(c, factor)
    return Color3.new(
        math.min(c.R * factor, 1),
        math.min(c.G * factor, 1),
        math.min(c.B * factor, 1)
    )
end

function ESPLib:_apply_chams(entry, model, is_visible)
    local ch = self.style.chams
    if not ch.enabled then
        for _, pair in pairs(entry.adornments) do
            for _, a in ipairs(pair) do
                if a and a.Parent then a:Destroy() end
            end
        end
        entry.adornments = {}
        return
    end
    local color = is_visible and ch.visible_color or ch.hidden_color
    local trans = is_visible and ch.visible_transparency or ch.hidden_transparency
    local glow_color = color_multiply(color, 5)
    local seen = {}

    for _, part_name in ipairs(BODY_PARTS) do
        local part = model:FindFirstChild(part_name)
        if part and part:IsA("BasePart") and part.Transparency < 1 then
            seen[part] = true
            local existing = entry.adornments[part]
            local glow_ad = existing and existing[1]
            local main_ad = existing and existing[2]
            local is_head = (part.Name == "Head") or (part.Name == "FakeHead")

            if ch.style == "Glow" then
                if not (glow_ad and glow_ad.Parent) then
                    glow_ad = Instance.new("BoxHandleAdornment")
                    glow_ad.Name = "_g"
                    glow_ad.AlwaysOnTop = true
                    glow_ad.ZIndex = is_head and 10 or 9
                    glow_ad.Adornee = part
                    glow_ad.Size = part.Size + Vector3.new(0.03, 0.03, 0.03)
                    glow_ad.Color3 = glow_color
                    glow_ad.Transparency = -1
                    pcall(function() glow_ad.Shading = Enum.AdornShading.XRayShaded end)
                    glow_ad.Parent = part
                else
                    glow_ad.Color3 = glow_color
                    glow_ad.Size = part.Size + Vector3.new(0.03, 0.03, 0.03)
                end
                if main_ad and main_ad.Parent then main_ad:Destroy(); main_ad = nil end
            else
                if not (main_ad and main_ad.Parent) then
                    main_ad = Instance.new("BoxHandleAdornment")
                    main_ad.Name = "_m"
                    main_ad.AlwaysOnTop = true
                    main_ad.ZIndex = 10
                    main_ad.Adornee = part
                    main_ad.Size = part.Size + Vector3.new(0.02, 0.02, 0.02)
                    main_ad.Color3 = color
                    main_ad.Transparency = trans
                    main_ad.Parent = part
                else
                    main_ad.Color3 = color
                    main_ad.Transparency = trans
                    main_ad.Size = part.Size + Vector3.new(0.02, 0.02, 0.02)
                end
                if glow_ad and glow_ad.Parent then glow_ad:Destroy(); glow_ad = nil end
            end
            entry.adornments[part] = { glow_ad, main_ad }
        end
    end

    for part, pair in pairs(entry.adornments) do
        if not seen[part] then
            for _, a in ipairs(pair) do
                if a and a.Parent then a:Destroy() end
            end
            entry.adornments[part] = nil
        end
    end
end

function ESPLib:_get_color(base_color, is_visible, is_hitscan)
    if is_hitscan and self.style.hitscan_color then return self.style.hitscan_color end
    if is_visible and self.style.visible_color then return self.style.visible_color end
    return base_color
end

function ESPLib:_project_corners(cf, size)
    local xs, ys, zs = size.X * 0.5, size.Y * 0.5, size.Z * 0.5
    local corners = {
        cf * Vector3.new(-xs, -ys, -zs),
        cf * Vector3.new( xs, -ys, -zs),
        cf * Vector3.new(-xs,  ys, -zs),
        cf * Vector3.new( xs,  ys, -zs),
        cf * Vector3.new(-xs, -ys,  zs),
        cf * Vector3.new( xs, -ys,  zs),
        cf * Vector3.new(-xs,  ys,  zs),
        cf * Vector3.new( xs,  ys,  zs),
    }
    local min_x, min_y, max_x, max_y = math.huge, math.huge, -math.huge, -math.huge
    local any_on = false
    for i = 1, 8 do
        local sp, on = wtvp(corners[i])
        if on then any_on = true end
        if sp.X < min_x then min_x = sp.X end
        if sp.Y < min_y then min_y = sp.Y end
        if sp.X > max_x then max_x = sp.X end
        if sp.Y > max_y then max_y = sp.Y end
    end
    if not any_on then return nil end
    return Vector2.new(min_x, min_y), Vector2.new(max_x - min_x, max_y - min_y)
end

local function lerp_color(c1, c2, t)
    return Color3.new(c1.R + (c2.R - c1.R) * t, c1.G + (c2.G - c1.G) * t, c1.B + (c2.B - c1.B) * t)
end

function ESPLib:_draw_hp(entry, pos, sz, hp, max_hp)
    local h = self.style.health
    if not h.enabled or max_hp <= 0 then
        entry.objs.hp_bar.Visible = false
        entry.objs.hp_outline.Visible = false
        entry.objs.hp_text.Visible = false
        return
    end
    local ratio = math.clamp(hp / max_hp, 0, 1)
    local bar_h = sz.Y
    local x = pos.X - 4 - h.thickness
    local top = Vector2.new(x, pos.Y)
    local bot = Vector2.new(x, pos.Y + bar_h)
    local fill_top = Vector2.new(x, pos.Y + bar_h * (1 - ratio))

    local ob = entry.objs.hp_outline
    ob.From = Vector2.new(x, pos.Y - 1)
    ob.To = Vector2.new(x, pos.Y + bar_h + 1)
    ob.Thickness = h.thickness + h.outline_thickness * 2
    ob.Color = h.outline_color
    ob.Visible = true

    local bar = entry.objs.hp_bar
    bar.From = fill_top
    bar.To = bot
    bar.Thickness = h.thickness
    bar.Color = lerp_color(h.color_low, h.color_full, ratio)
    bar.Visible = true

    if h.show_text then
        local tx = entry.objs.hp_text
        tx.Text = tostring(math.floor(hp)) .. "/" .. tostring(math.floor(max_hp))
        tx.Size = h.text_size
        tx.Font = h.text_font
        tx.Color = Color3.new(1,1,1)
        tx.Outline = true
        tx.OutlineColor = Color3.new(0,0,0)
        tx.Position = Vector2.new(x - 2, fill_top.Y - h.text_size / 2)
        tx.Visible = true
    else
        entry.objs.hp_text.Visible = false
    end
end

function ESPLib:_draw_text_stack(entry, pos, sz, model, hum, dist, is_visible)
    local stack_top = {}
    local stack_bot = {}

    if self.style.name.enabled then
        local n = entry.objs.name
        n.Text = entry.is_npc and model.Name or entry.subject.Name
        n.Size = self.style.name.size
        n.Font = self.style.name.font
        n.Color = self:_get_color(self.style.name.color, is_visible, false)
        n.Outline = self.style.name.outline
        n.OutlineColor = self.style.name.outline_color
        table.insert(stack_top, n)
    else
        entry.objs.name.Visible = false
    end

    if self.style.displayname.enabled and not entry.is_npc then
        local d = entry.objs.displayname
        d.Text = entry.subject.DisplayName
        d.Size = self.style.displayname.size
        d.Font = self.style.displayname.font
        d.Color = self.style.displayname.color
        d.Outline = self.style.displayname.outline
        d.OutlineColor = self.style.displayname.outline_color
        table.insert(stack_top, d)
    else
        entry.objs.displayname.Visible = false
    end

    if self.style.distance.enabled then
        local ds = entry.objs.distance
        ds.Text = tostring(math.floor(dist)) .. (self.style.distance.suffix or "")
        ds.Size = self.style.distance.size
        ds.Font = self.style.distance.font
        ds.Color = self.style.distance.color
        ds.Outline = self.style.distance.outline
        ds.OutlineColor = self.style.distance.outline_color
        table.insert(stack_bot, ds)
    else
        entry.objs.distance.Visible = false
    end

    if self.style.weapon.enabled then
        local w = entry.objs.weapon
        local wname = ""
        if self._weapon_getter then
            local ok, r = pcall(self._weapon_getter, entry.subject, model)
            if ok and r then wname = tostring(r) end
        end
        w.Text = wname
        w.Size = self.style.weapon.size
        w.Font = self.style.weapon.font
        w.Color = self.style.weapon.color
        w.Outline = self.style.weapon.outline
        w.OutlineColor = self.style.weapon.outline_color
        if wname ~= "" then table.insert(stack_bot, w) else w.Visible = false end
    else
        entry.objs.weapon.Visible = false
    end

    local center_x = pos.X + sz.X * 0.5
    local y = pos.Y - 2
    for i = #stack_top, 1, -1 do
        local obj = stack_top[i]
        y = y - obj.Size - 1
        obj.Position = Vector2.new(center_x, y)
        obj.Visible = true
    end
    local y2 = pos.Y + sz.Y + 2
    for _, obj in ipairs(stack_bot) do
        obj.Position = Vector2.new(center_x, y2)
        obj.Visible = true
        y2 = y2 + obj.Size + 1
    end
end

function ESPLib:_draw_box(entry, pos, sz, is_visible)
    if not self.style.box.enabled and not self.style.box_outline.enabled and not self.style.box_fill.enabled then
        entry.objs.box.Visible = false
        entry.objs.box_outline.Visible = false
        entry.objs.box_fill.Visible = false
        return
    end
    if self.style.box_fill.enabled then
        local f = entry.objs.box_fill
        f.Size = sz
        f.Position = pos
        f.Color = self.style.box_fill.color
        f.Transparency = self.style.box_fill.transparency
        f.Visible = true
    else
        entry.objs.box_fill.Visible = false
    end
    if self.style.box_outline.enabled then
        local o = entry.objs.box_outline
        o.Size = sz
        o.Position = pos
        o.Thickness = self.style.box_outline.thickness
        o.Color = self.style.box_outline.color
        o.Transparency = self.style.box_outline.transparency
        o.Visible = true
    else
        entry.objs.box_outline.Visible = false
    end
    if self.style.box.enabled then
        local b = entry.objs.box
        b.Size = sz
        b.Position = pos
        b.Thickness = self.style.box.thickness
        b.Color = self:_get_color(self.style.box.color, is_visible, false)
        b.Transparency = self.style.box.transparency
        b.Visible = true
    else
        entry.objs.box.Visible = false
    end
end

function ESPLib:_draw_skeleton(entry, model)
    self:_ensure_skel(entry)
    if not self.style.skeleton.enabled then return end
    for i, pair in ipairs(skeleton_bones) do
        local ln = entry.skel_lines[i]
        if not ln then continue end
        local a = model:FindFirstChild(pair[1])
        local b = model:FindFirstChild(pair[2])
        if a and b and a:IsA("BasePart") and b:IsA("BasePart") then
            local sa, oa = wtvp(a.Position)
            local sb, ob = wtvp(b.Position)
            if oa and ob then
                ln.From = Vector2.new(sa.X, sa.Y)
                ln.To = Vector2.new(sb.X, sb.Y)
                ln.Thickness = self.style.skeleton.thickness
                ln.Color = self.style.skeleton.color
                ln.Visible = true
            else
                ln.Visible = false
            end
        else
            ln.Visible = false
        end
    end
end

function ESPLib:_draw_arrow(entry, model)
    local a = self.style.off_screen_arrow
    if not a.enabled then
        entry.objs.arrow.Visible = false
        return
    end
    local hum, hrp = get_hum_hrp(model)
    if not hrp then
        entry.objs.arrow.Visible = false
        return
    end
    local sp, on = wtvp(hrp.Position)
    if on then
        entry.objs.arrow.Visible = false
        return
    end
    local vp = Camera.ViewportSize
    local cx, cy = vp.X * 0.5, vp.Y * 0.5
    local dx, dy = sp.X - cx, sp.Y - cy
    if sp.Z < 0 then dx, dy = -dx, -dy end
    local angle = math.atan2(dy, dx)
    local px = cx + math.cos(angle) * a.radius
    local py = cy + math.sin(angle) * a.radius
    local sz = a.size
    local tri = entry.objs.arrow
    tri.PointA = Vector2.new(px + math.cos(angle) * sz, py + math.sin(angle) * sz)
    tri.PointB = Vector2.new(px + math.cos(angle + 2.6) * sz * 0.6, py + math.sin(angle + 2.6) * sz * 0.6)
    tri.PointC = Vector2.new(px + math.cos(angle - 2.6) * sz * 0.6, py + math.sin(angle - 2.6) * sz * 0.6)
    tri.Color = a.color
    tri.Visible = true
end

function ESPLib:_hide_entry(entry)
    for _, obj in pairs(entry.objs) do obj.Visible = false end
    for _, ln in pairs(entry.skel_lines) do ln.Visible = false end
end

function ESPLib:_update_entry(entry)
    if not self.style.enabled then
        self:_hide_entry(entry)
        return
    end
    local model = self:_get_model(entry.subject, entry.is_npc)
    if not model then self:_hide_entry(entry); return end
    local hum, hrp = get_hum_hrp(model)
    if not hum then self:_hide_entry(entry); return end

    local lp_char = LocalPlayer.Character
    local lp_hrp = lp_char and lp_char:FindFirstChild("HumanoidRootPart")
    local dist = lp_hrp and (lp_hrp.Position - hrp.Position).Magnitude or 0
    if dist > self.style.max_distance then
        self:_hide_entry(entry)
        self:_draw_arrow(entry, model)
        return
    end

    local ok, cf, size = pcall(model.GetBoundingBox, model)
    if not ok or not size then self:_hide_entry(entry); return end
    local pos, sz = self:_project_corners(cf, size)
    if not pos then
        self:_hide_entry(entry)
        self:_draw_arrow(entry, model)
        return
    end
    entry.objs.arrow.Visible = false

    local is_visible = false
    if self._visible_check_fn then
        local ok2, vis = pcall(self._visible_check_fn, entry.subject, model, hrp.Position)
        if ok2 then is_visible = vis == true end
    end
    local is_hitscan = false
    if self._hitscan_check_fn then
        local ok3, hs = pcall(self._hitscan_check_fn, entry.subject, model, hrp.Position)
        if ok3 then is_hitscan = hs == true end
    end

    self:_apply_chams(entry, model, is_visible)
    self:_draw_box(entry, pos, sz, is_visible)
    self:_draw_hp(entry, pos, sz, hum.Health, hum.MaxHealth)
    self:_draw_text_stack(entry, pos, sz, model, hum, dist, is_visible)
    self:_draw_skeleton(entry, model)
end

function ESPLib:Add(subject, is_npc)
    if not subject then return end
    if self._entries[subject] then return self._entries[subject] end
    if not self:_should_track(subject, is_npc) then return end
    local entry = self:_create_entry(subject, is_npc)
    self._entries[subject] = entry
    return entry
end

function ESPLib:Remove(subject)
    local entry = self._entries[subject]
    if not entry then return end
    self:_destroy_entry(entry)
    self._entries[subject] = nil
end

function ESPLib:GetEntry(subject) return self._entries[subject] end

function ESPLib:UpdateStyle(patch)
    if not patch then return end
    deep_merge(self.style, patch)
end

function ESPLib:Set(path, value)
    local parts = {}
    for p in string.gmatch(path, "[^%.]+") do table.insert(parts, p) end
    local node = self.style
    for i = 1, #parts - 1 do
        node = node[parts[i]]
        if type(node) ~= "table" then return end
    end
    node[parts[#parts]] = value
end

function ESPLib:_scan_loot(now)
    local l = self.style.loot
    if not l.enabled then
        for _, e in pairs(self._loot_entries) do
            if e.text then e.text.Visible = false end
        end
        return
    end
    if now - self._last_loot_scan < 0.5 then return end
    self._last_loot_scan = now
    local root
    if self._loot_root_getter then
        local ok, r = pcall(self._loot_root_getter)
        if ok then root = r end
    else
        root = Workspace:FindFirstChild("Items") or Workspace:FindFirstChild("Drops") or Workspace:FindFirstChild("Loot")
    end
    if not root then return end
    local seen = {}
    for _, inst in ipairs(root:GetChildren()) do
        if inst:IsA("Model") or inst:IsA("BasePart") then seen[inst] = true end
    end
    for inst, e in pairs(self._loot_entries) do
        if not seen[inst] then
            if e.text then e.text:Remove() end
            self._loot_entries[inst] = nil
        end
    end
    for inst in pairs(seen) do
        if not self._loot_entries[inst] then
            local t = new_drawing("Text", {
                Center = true, Visible = false,
                Size = l.text_size, Font = l.text_font,
                Color = l.color, Outline = l.outline, OutlineColor = l.outline_color,
                Text = inst.Name,
            })
            self._loot_entries[inst] = { text = t, inst = inst, name = inst.Name, worth = 0 }
        end
    end
end

function ESPLib:_render_loot()
    local l = self.style.loot
    if not l.enabled then return end
    local lp = LocalPlayer.Character
    local lp_pos = lp and lp:FindFirstChild("HumanoidRootPart") and lp.HumanoidRootPart.Position
    for inst, e in pairs(self._loot_entries) do
        local part = inst:IsA("Model") and (inst.PrimaryPart or inst:FindFirstChildWhichIsA("BasePart")) or inst
        if not part or not part.Parent then
            e.text.Visible = false
        else
            local dist = lp_pos and (part.Position - lp_pos).Magnitude or 0
            if dist > l.max_distance then
                e.text.Visible = false
            else
                if self._loot_worth_getter then
                    local ok, w = pcall(self._loot_worth_getter, inst)
                    if ok then e.worth = tonumber(w) or 0 end
                end
                if l.min_worth > 0 and e.worth < l.min_worth then
                    e.text.Visible = false
                else
                    local sp, on = wtvp(part.Position)
                    if not on then
                        e.text.Visible = false
                    else
                        local txt = e.name
                        if l.show_distance then txt = txt .. " [" .. math.floor(dist) .. "m]" end
                        if e.worth > 0 then txt = txt .. " $" .. e.worth end
                        e.text.Text = txt
                        e.text.Color = l.color
                        e.text.Position = Vector2.new(sp.X, sp.Y)
                        e.text.Visible = true
                    end
                end
            end
        end
    end
end

function ESPLib:_update_trails(now)
    local tr = self.style.trail
    if not tr.enabled then
        for _, td in pairs(self._trail_data) do
            for _, ln in ipairs(td.lines) do ln.Visible = false end
        end
        return
    end
    for subject, entry in pairs(self._entries) do
        local model = self:_get_model(subject, entry.is_npc)
        if model then
            local _, hrp = get_hum_hrp(model)
            if hrp then
                local pos = hrp.Position
                local td = self._trail_data[subject]
                if not td then
                    td = { points = {}, lines = {} }
                    self._trail_data[subject] = td
                end
                table.insert(td.points, { pos = pos, t = now })
                while #td.points > tr.max_segments do table.remove(td.points, 1) end
                while td.points[1] and (now - td.points[1].t) > tr.segment_life do
                    table.remove(td.points, 1)
                end
                while #td.lines < #td.points - 1 do
                    table.insert(td.lines, new_drawing("Line", { Visible = false, Thickness = tr.thickness, Color = tr.color, Transparency = 1 - tr.transparency }))
                end
                while #td.lines > #td.points - 1 do
                    local ln = table.remove(td.lines)
                    if ln then ln:Remove() end
                end
                for i = 1, #td.points - 1 do
                    local sp1, o1 = wtvp(td.points[i].pos)
                    local sp2, o2 = wtvp(td.points[i + 1].pos)
                    local ln = td.lines[i]
                    if ln and o1 and o2 then
                        ln.From = Vector2.new(sp1.X, sp1.Y)
                        ln.To = Vector2.new(sp2.X, sp2.Y)
                        ln.Color = tr.color
                        ln.Thickness = tr.thickness
                        ln.Transparency = 1 - tr.transparency
                        ln.Visible = true
                    elseif ln then
                        ln.Visible = false
                    end
                end
            end
        end
    end
    for subject, td in pairs(self._trail_data) do
        if not self._entries[subject] then
            for _, ln in ipairs(td.lines) do
                if ln then ln:Remove() end
            end
            self._trail_data[subject] = nil
        end
    end
end

function ESPLib:Start()
    if self._running then return end
    self._running = true

    local pa = Players.PlayerAdded:Connect(function(p) self:Add(p, false) end)
    local pr = Players.PlayerRemoving:Connect(function(p) self:Remove(p) end)
    table.insert(self._connections, pa)
    table.insert(self._connections, pr)
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then self:Add(p, false) end
    end

    local ai_folder = Workspace:FindFirstChild("AiZones") or Workspace:FindFirstChild("AI")
    if ai_folder then
        for _, m in ipairs(ai_folder:GetDescendants()) do
            if m:IsA("Model") and m:FindFirstChildOfClass("Humanoid") then
                self:Add(m, true)
            end
        end
        local aa = ai_folder.DescendantAdded:Connect(function(m)
            if m:IsA("Model") and m:FindFirstChildOfClass("Humanoid") then self:Add(m, true) end
        end)
        local ar = ai_folder.DescendantRemoving:Connect(function(m)
            if self._entries[m] then self:Remove(m) end
        end)
        table.insert(self._connections, aa)
        table.insert(self._connections, ar)
    end

    local rs = RunService.RenderStepped:Connect(function()
        local now = tick()
        for _, entry in pairs(self._entries) do
            self:_update_entry(entry)
        end
        self:_scan_loot(now)
        self:_render_loot()
        self:_update_trails(now)
    end)
    table.insert(self._connections, rs)
end

function ESPLib:Stop()
    self._running = false
    for _, c in ipairs(self._connections) do
        c:Disconnect()
    end
    self._connections = {}
    for subject, e in pairs(self._entries) do
        self:_destroy_entry(e)
        self._entries[subject] = nil
    end
    for _, e in pairs(self._loot_entries) do
        if e.text then e.text:Remove() end
    end
    self._loot_entries = {}
    for _, td in pairs(self._trail_data) do
        for _, ln in ipairs(td.lines) do
            if ln then ln:Remove() end
        end
    end
    self._trail_data = {}
end

return ESPLib
